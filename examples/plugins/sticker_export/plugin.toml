[plugin]
name = "sticker_export"
version = "0.1.0"
description = "Cross-platform sticker export for QQ and Telegram"
authors = ["Vollate"]
license = "MIT"

[compatibility]
obcx_abi_version = 1
obcx_min_version = "1.1.0"

[dependencies]
required_plugins = []

[build]
vcpkg_deps = []
