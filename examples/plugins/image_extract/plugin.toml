[plugin]
name = "image_extract"
version = "0.1.0"
description = "Image extraction and processing from messages"
authors = ["Vollate"]
license = "MIT"

[compatibility]
obcx_abi_version = 1
obcx_min_version = "1.1.0"

[dependencies]
required_plugins = []

[build]
vcpkg_deps = []
