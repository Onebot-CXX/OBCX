[plugin]
name = "exhentai_fetcher"
version = "0.1.0"
description = "ExHentai content fetcher and parser"
authors = ["Vollate"]
license = "MIT"

[compatibility]
obcx_abi_version = 1
obcx_min_version = "1.1.0"

[dependencies]
required_plugins = []

[build]
vcpkg_deps = ["libxml2"]
