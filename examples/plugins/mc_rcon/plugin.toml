[plugin]
name = "mc_rcon"
version = "0.1.0"
description = "Minecraft RCON client for executing server commands via QQ"
authors = ["Vollate"]
license = "MIT"

[compatibility]
obcx_abi_version = 1
obcx_min_version = "1.1.0"

[dependencies]
required_plugins = []

[build]
vcpkg_deps = ["tomlplusplus"]
