# plugins.toml — Edit this file to control which plugins are compiled
# Local plugins: set enabled = false to skip compilation
# Remote plugins: add "user/repo" strings to the array

# ===== Local plugins (in examples/plugins/) =====
[local]
plugins = [
  # { name = "bridge_bot", path = "examples/plugins/bridge_bot" },
  # { name = "torrent_downloader", path = "examples/plugins/torrent_downloader" },
  # { name = "mc_rcon", path = "examples/plugins/mc_rcon" },
  # { name = "image_extract", path = "examples/plugins/image_extract" },
  # { name = "group_member_backup", path = "examples/plugins/group_member_backup" },
  # { name = "chat_llm", path = "examples/plugins/chat_llm" },
  # { name = "exhentai_fetcher", path = "examples/plugins/exhentai_fetcher" },
  # { name = "sticker_export", path = "examples/plugins/sticker_export" },
]

# ===== Remote plugins (GitHub repos, just "user/repo") =====
[remote]
plugins = [
  "vollate/obcx-plugin-mc-rcon",
]

# Optional: pin specific versions for remote plugins
# [remote.overrides."user/repo"]
# tag = "v1.0.0"
# branch = "main"
