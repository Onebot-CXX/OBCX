## 1. Configuration parsing and validation

- [x] 1.1 Parse overrides by canonical table key and validate the keyed shape, required policies, bounds, and legacy syntax rejection.
- [x] 1.2 Migrate and extend configuration tests for multiple keyed overrides, invalid shapes/names/fields, duplicate tables, and limits.

## 2. Migration and verification

- [x] 2.1 Migrate command routing/help fixtures and verify replacement, alias, inactive-name, and help-filter behavior.
- [x] 2.2 Update operator documentation and development configuration to command-keyed headings without changing permissions.
- [x] 2.3 Format changed C++ files, build with available cores, run focused tests in parallel, and validate the change artifacts and development configuration.
